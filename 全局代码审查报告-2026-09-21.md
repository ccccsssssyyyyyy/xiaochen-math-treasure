# 全局代码审查报告（2026-09-21）

范围：小陈的数学宝藏 v2.4.1。聚焦你点名的三件事——用得顺、数据安全可备份、题目不丢。

结论先说：**数据安全与「题目不丢」的主干设计非常扎实**，没有会静默删库的致命漏洞；发现 1 个真实隐患（孤儿图清理会误删在用题图）和 2 个需补强的点。

**当前状态：P1 / P2（每日备份）/ P3（选 B，加导入入口）/ 体验项 2 全部已改完并验证。**

---

## 一、数据安全 / 备份 / 题目不丢 —— 主干强

| 环节 | 实现 | 评价 |
|---|---|---|
| 库迁移 | `db_migrations.py`：升级前 `create_pre_migration_backup`（一致性 `source.backup` + `integrity_check` + 转 DELETE 独立模式 + 删 WAL 侧文件 + sha256），逐级只加表/加列，无任何 DROP/TRUNCATE | 强，fail-closed |
| 启动校验 | `init_db()`：版本高于程序支持即拒绝启动；缺核心表/核心列即拒绝启动，绝不拿 create_all 掩盖损坏 | 强，防降级毁库 |
| 完整备份 | `mathbank/backup.py`：一致性拷贝→独立库→完整性+外键校验→按引用归集图片→manifest 带 sha256→原子写 zip→写后回校→保留 10 份 | 强 |
| 还原 | `restore_full_backup`：持 runtime 锁→先存原始字节恢复包→校验→换库前再存安全备份→临时名暂存→`os.replace` 原子替换→失败回滚→TTL 待还原请求（重启窗口落地） | 强 |
| 导入去重 | 入库前 `find_duplicate_question`，命中返回 `duplicate_warning_payload`（409），**不覆盖、不删旧题**；错题批量入库逐题回传重复，不静默丢 | 强，重导安全 |
| 删除边界 | 无「清空题库」类端点；删组卷只级联删 `paper_questions` 关联，**不碰 questions 本体** | 强 |
| 可读导出 | `export_database_to_files`：每次写库后原子导出 JSON + Markdown 全量题库，作额外可读兜底 | 加分项 |
| 分发干净 | `build_release.py` 排除 `.db`/`.db-wal`/`.db-shm` 与 `data_backup` | 强，不泄漏用户数据 |

这套机制对你担心的「前期积累题目消失」基本兜住了：坏库启动不了、版本错位启动不了、导入重复不覆盖、删卷不删题、还原有回滚。

---

## 二、发现的问题 —— 已全部修完

### P1 孤儿图清理会误删在用题图 ✅ 已修

文件：`main.py` 的 `clean_orphaned_images()`，每次启动执行。

问题：它只从 `Question.image_paths` 收集引用，**没看**`content` / `answer_markdown` 里的内联 `![alt](url)`，也**没看**错题记录的插图。而备份校验用的是全源扫描。两条路口径不一致 → 只在正文里引用的图，启动后被当「无人引用」删掉。

修法（已落地）：
- 新建 `mathbank/upload_refs.py`，把「哪些上传图还在用」定义成**一份契约**（`REFERENCE_TABLES`：questions 的 `image_paths`/`content`/`answer_markdown` + mistake_records 的 `block_images`/`image_block`/`image_figure`/`answer_image`）。
- 引用收集器 `iter_upload_references()` 同时兼容 SQLAlchemy 连接与裸 sqlite3；备份那一路 fail-closed（读不了一行就报错），清理那一路跳过坏行。
- `main.py:clean_orphaned_images` 与 `mathbank/backup.py:_database_upload_references` 现在共用它。**读不到引用集就不清理**（保险），宁可留垃圾图也不误删。
- 回归测试 3 条（`tests/test_sync.py`）：正文内联图保留、解析内联图保留、错题块图保留、真孤儿仍删。

### P2 自动备份只在启动时触发一次 ✅ 已修（每日滚动）

问题：`create_full_backup_if_due` 只在启动 2.5 秒后跑一次。服务连开数周不重启，期间只有启动那一份快照。

修法（已落地）：`main.py` 加 `DAILY_BACKUP_CHECK_SECONDS = 1 小时` 的常驻循环（`start_daily_backup_scheduler`），每小时问一次 `create_full_backup_if_due`（内部仍是 24 小时间隔 + 保留 10 份），异常吞掉不打断服务；测试环境不启。回归测试：`test_run_daily_backup_once_triggers_scheduler_and_swallows_errors`。

### P2′ 便携包升级的数据归属风险 ✅ 已修（按你选的 B）

风险：新版解压到**新文件夹**再打开，看到的是空题库（旧库留在老文件夹）——表现上就是「题目全没了」。

修法（已落地，B 方案：加导入入口）：
- `mathbank/backup.py` 新增 `resolve_legacy_source()` / `validate_legacy_database()` / `import_legacy_database()`。旧库以**只读**方式打开，做 `integrity_check`、必填表检查、版本不得高于程序支持；通过后打包成一份**标准完整备份**（走现成的 `create_full_backup` 校验链路）。
- `main.py` 新增 `POST /api/backup/import-legacy-database`（需 `confirm:true`）：打包 → `create_pre_restore_backup` → `write_pending_restore` → 重启落地。**零新增安全关键还原逻辑**，复用已有的延迟还原通道。
- 前端 `static/index.html` + `static/js/backup.js`：备份面板里加「从旧版程序目录导入题库」，填路径 + 二次确认。
- 回归测试 5 条（`tests/test_backup_restore_api.py`）：缺确认 400 / 路径不存在 422 / 缺表 422 / 打包并登记换库端到端 / 缺令牌 403。

---

## 三、可用性

### 体验项 2：屏幕图位与 PDF 不一致 ✅ 已修（按你选的 B，网页同步）

原先：屏幕上图留在正文出现的原位，试卷 PDF 把图抽到「题干之后、选项之前」并居中（`paper_helper.py`：`stem_text → 图 → choices_part`）。录入对错是**对着印出来的重做卷**判的，同一道题两个位置，看着不像同一张卷。

修法（已落地）：
- `static/js/math-render.js` 的 `renderQuestionBody` 支持 `stripFigures`：图从正文流里摘掉，只登记 URL，交给版式层统一摆。三处去向（渲染 / 摘图 / 保留 markdown）收在一个函数里，不拆两份。
- 顺手修掉 `inlineFigureUrls()` 的一个真 bug：它复用 `replaceInlineFigures(...).urls`，而那里只在「被换成了别的东西」时才收集 URL —— 原样返回 match 的调用被判成「没渲染」，于是**永远返回空数组**。
- `static/js/redo.js`：题干先按正文出现顺序收集图，与 `image_paths` 合并去重，再插在**选项块之前**、居中。选项里带的图同样被抽出来（与 PDF 对整段 content 摘图同口径）。解析区的图仍就地显示 —— PDF 对 `answer_markdown` 本来就是就地转 `\includegraphics`。
- 为什么不做 `figure_align` 的右侧/居右分支：库里 1399 道题有 1398 条 `figure_align` 是 `'right'`，而那是**旧列默认值的残留**；`paper_helper.py` 与 `paper.js:getQuestionFigAlign` 都把它归一成 `center`，所以印出来实际全是题末居中（另 1 条是 `center`，`bottom_right` 为 0）。照 `right` 字面值渲染反而会把绝大多数题排错。

### 仍待你拍板（未动）

1. **49 题 `image_paths` 有图但正文没有**（导入残留，如 id=1370 填空题挂图）—— 建议暂不动，见原审查结论。
2. `POST /api/redo/decline` 孤儿接口（前端已无调用）保留未删，先前已记为待定。
3. **20 道题的题源不符合命名规约**：`tests/test_source_normalize.py::test_all_db_sources_are_canonical` 是红的（全量唯一失败项），值 = `高一上第1-2周练习`(15 题) + `高一上第3周`(5 题)。这条你在 2026-09-20 已明确**暂不归一**（记录在 `docs/错题重做闭环-实施计划-2026-09-20.md` §8.14，CI 无库会自动跳过），不是本次改动引入，列在这里只为「全量红一条」有出处。
4. **新发现（小）：在组卷台选过「题干右侧」保存不住**。`custom_figure_align` 只存在前端内存（`PaperStore.questionsMap`），刷新页面后 `getQuestionFigAlign` 只见 `figure_align='right'`，按「旧默认残留」归一回居中；导出 PDF 也跟着回居中 —— 也就是说选中「题干右侧」当场生效、刷新即失效。要保留就得把「用户显式选过」这个状态落库。这条未记录在案，需你定要不要修。

### 验证证据

| 项目 | 结果 |
|---|---|
| 前端语法 | `node --check static/js/*.js` 10/10 通过 |
| 重做录入页运行期夹具 | `tests/js/redo_payload_check.js` **125 项全通过**（原 122 项，图位新增/改写 16 项在列） |
| 相关套件 | `test_redo_frontend` 25 passed；`test_mistake_frontend` + `test_backup_panel` + `test_backup_restore_api` 98 passed；`test_sync` 7 passed；`test_frontend_security` + `test_katex_math_render` 25 passed |
| **全量** | **1229 passed / 4 skipped / 1 failed** —— 唯一失败项就是上面第 3 条的库内题源规约（数据问题，非本次改动引入） |
| 真实数据端到端 | 线上库拷副本 + 真 `GET /api/redo/papers/4` → 真 payload 喂真 `redo.js`（vm 假 DOM）：**19/19 通过**。那份物理重做卷 20 题、14 题含正文内联图，图全出、无 markdown 残留、无重复、无死链，配图块恰好一个且排在选项 A. 之前；生产库行数与累计错次零改动 |

> 沙箱提示：跑 pytest 前设一个干净的 `TMPDIR`（如 `TMPDIR=/tmp/xx`）。残留的 `pytest-of-unknown` 目录会让用例在 setup 阶段报 `PermissionError: EEXIST`，看起来像真实回归。

---

## 四、尚未提交

以上改动都还在工作区，**没有 commit / push**。等你确认后再提交（可以按主题拆几笔：数据安全口径 / 每日备份 / 导入入口 / 图位口径）。
