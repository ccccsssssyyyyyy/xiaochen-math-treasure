# 一键安装指令（给 AI 助手执行）

不想碰命令行？把下面这段**整段复制**，粘贴给你的 AI 助手——WorkBuddy、Claude Code，或任何能操作你电脑的 AI 助手——它会自动完成「下载 → 解压 → 启动」全过程。

> 前提：你的 AI 助手必须能**在你的电脑上执行命令**。纯网页版聊天机器人（豆包、Kimi 网页版等）做不到。
>
> 全程不需要你敲任何命令，也不需要你预先安装 Python：安装包自带运行时，解压双击即跑。

---

````text
请直接在我的电脑上操作，帮我把「小陈的数学宝藏」装好并打开。它是一个本地数学题库工具（数学老师用来录题、AI 拆卷、组卷出卷），数据只存在本机，不需要注册和登录。

请全自动完成，每完成一步用一句话告诉我进度。任何一步失败，请把完整报错原文贴出来，不要跳过继续，也不要静默重试。

第 1 步｜确认系统与芯片
告诉我我用的是 macOS 还是 Windows。
如果是 macOS，再判断芯片：执行 uname -m，输出 arm64 说明是 Apple 芯片，输出 x86_64 说明是 Intel 芯片（若在 Rosetta 下运行，请以 sysctl -n hw.optional.arm64 是否为 1 为准）。

第 2 步｜下载
从 GitHub 官方发布页下载对应安装包（免登录）：
· macOS（Apple 芯片）：https://github.com/ccccsssssyyyyyy/xiaochen-math-treasure/releases/latest/download/xiaochen-math-treasure-macOS-AppleSilicon.zip
· macOS（Intel 芯片）：https://github.com/ccccsssssyyyyyy/xiaochen-math-treasure/releases/latest/download/xiaochen-math-treasure-macOS-Intel.zip
· Windows：https://github.com/ccccsssssyyyyyy/xiaochen-math-treasure/releases/latest/download/xiaochen-math-treasure-Windows-x64.zip
如果下载失败或特别慢，改用国内镜像重试——在原地址前面加上 https://gh-proxy.com/ ，并告诉我最后用的是哪个地址。
如果上面某个直链返回 404（说明该版本的资源命名变了），请改为打开 https://github.com/ccccsssssyyyyyy/xiaochen-math-treasure/releases/latest 这个页面，在里面挑选与我系统匹配的那个 zip 包下载，并告诉我你选的包名。

第 3 步｜解压
解压到我的「文档 / 文稿」目录，文件夹命名为「小陈的数学宝藏」。解压后应当能看到 main.py 和「启动题库系统.command」（Windows 是「启动题库系统.bat」）。
macOS 上如果「启动题库系统.command」不可双击运行，请执行 chmod +x "启动题库系统.command" 后再试。

第 4 步｜解除 macOS 下载隔离（仅 macOS，重要）
从浏览器下载的压缩包会被 macOS 打上「隔离属性」，它会连带阻止安装包内自带的 Python 运行时被启动，典型表现是双击启动器后弹窗提示「Apple 无法验证」或「无法打开，因为无法验证开发者」。
请在解压目录的上一级执行（把路径换成实际解压目录）：
xattr -dr com.apple.quarantine "/路径/小陈的数学宝藏"
如果 xattr 报「Operation not permitted」或没有 xattr 命令，就走手动方式：在访达里按住 Control 点（或右键）「启动题库系统.command」→ 选「打开」→ 在弹窗里再点一次「打开」。
如果跳过这一步，程序本身没有损坏——只是会被 macOS 的安全机制挡在门外。

第 5 步｜启动
· macOS：在解压目录里执行  open "启动题库系统.command"
· Windows：用系统方式打开「启动题库系统.bat」，例如  start "" "启动题库系统.bat"
务必用这种方式启动，不要在你自己的终端里前台运行 uvicorn —— 否则你的会话一结束，服务就会跟着退出。
这一步不需要 pip install，也不需要建 venv：安装包已在打包时装好全部依赖。若脚本提示内置运行时无法运行或架构不匹配，说明下载的包与我的机器不匹配，请按提示换另一个 macOS 包重来。

第 6 步｜验证
等它自动打开浏览器（地址 http://127.0.0.1:8000）。若 30 秒仍未打开，请查看解压目录下 .system_generated/server.log 里的报错并贴给我。

第 7 步｜收尾，告诉我四件事
① 网页有没有正常打开；
② 我怎样才能用上 AI 功能（我知道需要自己申请大模型的 API Key，请告诉我去哪申请、填在哪里）；
③ 现在能不能先不配 Key 就试一下（请告诉我页面上的「首次启动引导」卡片里有没有「载入示例题」按钮，它不需要 Key 就能试组卷）；
④ 以后我再用它，该怎么打开。
````

---

## 装好之后

1. **先不配 Key 也能试**：首次打开时题库是空的，页面顶部会出现「开始使用：三步就能出一份卷」引导卡片，点 **载入示例题** 会写入 8 道内置示例题（选择 / 多选 / 填空 / 解答齐全），可直接去组卷工作台练手——全程不需要 API Key。
2. **填 API Key**：点网页右上角 **⚙️ 设置** →「API 配置」，填入你自己的 DeepSeek / 阿里百炼 / 硅基流动等密钥并保存。
   - 不填也能用：录入、预览、检索、导出源码、示例题组卷都正常；
   - 想用 AI 拆卷 / 解题 / OCR，才必须有有效 Key。各平台免费额度与选型见 [README 的 API 配置章节](README.md#api-配置与大模型选型)。
   - 不知道该点哪家：在引导卡片上点「去哪申请」，或打开 **设置 → 关于 → 运行环境自检 → 查看安装指引**。
3. **导出 PDF 要装 LaTeX**（约 5GB，可后补）：不装也能用全部其它功能，只是「一键导出 PDF」与 TikZ 几何重绘不可用。缺什么、影响什么、怎么装，在 **设置 → 关于 → 运行环境自检** 里有逐项说明和国内镜像地址，PDF 导出失败时也会自动弹出这个面板。
4. **以后怎么再打开**：macOS 双击目录里的「启动题库系统.command」，Windows 双击「启动题库系统.bat」。浏览器会开到 <http://127.0.0.1:8000>。
5. **数据在哪**：全部在你解压出来的这个目录里（`math_question_bank.db` 与 `static/uploads/`）。换电脑时整个目录拷走即可；升级前建议先备份，详见 [README 的版本升级与数据备份](README.md#版本升级与数据备份)。

## macOS 首次打开被拦住？

从浏览器下载的 zip 会被 macOS 打上隔离属性，双击启动器可能提示「Apple 无法验证」或「无法打开，因为无法验证开发者」。这不是文件损坏，两条路任选：

1. **推荐**：终端里执行（换成你的实际路径）
   ```bash
   xattr -dr com.apple.quarantine "/Users/你的用户名/Documents/小陈的数学宝藏"
   ```
2. **不用终端**：在访达里按住 Control 点（或右键）「启动题库系统.command」→ 选「打开」→ 弹窗里再点一次「打开」。

启动器本身也会尝试一次性自愈（检测到隔离属性就自动解除），但**第一次双击之前它还没机会运行**，所以上面的步骤不能省。

## 不想用 AI，自己动手装

见 [README 的快速开始](README.md#快速开始)：方式一下载便携包，方式二源码运行。

## 关于本文件

本文件是给用户直接复制转发用的，不属于应用运行时代码。若你修改了上面的下载地址、启动方式或安装步骤，请同步更新 [README](README.md) 的「快速开始」与 [AGENTS.md](AGENTS.md) 的安装说明，避免三处说法不一致。
