# MathBank V2.1.4 更新日志

发布日期：2026-08-15

## 题型识别与确认流程

- 新增保守的题型识别机制：题干出现 `\fillin` 时直接识别为填空题，出现 `\begin{choices}` 时直接识别为选择题。
- 选择题中即使包含 `\fillin`，也优先按外层 `choices` 环境识别，避免选项内的空格误导题型。
- AI 只识别“选择题”这一粗粒度类型，不再猜测单选或多选；识别为选择题后，必须由用户手动确认“单选题”或“多选题”才能保存。
- 填空题和解答题会自动带入识别结果；只有结构与 AI 都无法可靠判断时，才显示“待手动确认”并保留当前题型。
- 修复 AI 未返回题型字段时，系统误将题目默认读取为解答题的问题。

## 交互体验优化

- 优化单选题与多选题的选中状态：选中后使用高对比品牌色背景、白色文字和勾选图标，鼠标悬停或按下时也不会变暗。
- 增加 `aria-checked` 状态，提升题型确认按钮对辅助技术的可读性。
- 修复弹窗打开时第一个操作按钮或右上角关闭按钮被自动高亮的问题。默认焦点现在落在弹窗标题或容器上，同时保留 Tab 键焦点循环、Esc 关闭、背景禁止聚焦和关闭后恢复焦点。
- OCR 弹窗的全局粘贴能力保持不变，用户仍可在打开后直接使用 `Ctrl+V` 或 `Cmd+V` 粘贴截图。

## 兼容性说明

- 本次更新不会自动改写题库中已保存题目的题型。如历史题目曾被误分类，请在编辑页面重新选择并保存。
- 当题干既没有标准的 `\fillin` / `choices` 结构，AI 结果也不可靠时，系统会保留用户已选题型，不会擅自改为解答题。

## 质量验证

- 后端、API、题型规则及前端交互回归测试全部通过。
- Python 依赖完整性、前端 JavaScript 语法与 macOS 启动脚本语法均已校验。
