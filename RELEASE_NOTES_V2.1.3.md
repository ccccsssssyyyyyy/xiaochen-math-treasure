# MathBank V2.1.3 更新日志

发布日期：2026-08-14

## Bug 修复

- 修复 macOS 上旧虚拟环境使用过低 Python 版本时无法启动的问题。当本机已有 Python 3.10 或更高版本时，启动器会自动备份并重建项目环境。
- 修复旧版 MathBank 未退出并占用 8000 端口时，新版启动器无法继续的问题。启动器现在会先确认该进程确实属于 MathBank，再安全停止；其他程序不会被误关闭。
- 修复部分 macOS Python 安装方式下，启动器无法正确识别已运行题库进程，导致重启失败的问题。
- 修复参考答案或解析中包含图片时，LaTeX/PDF 可能因图片宽度参数而编译失败的问题。
- 修复一道题同时融合多个教材模块时，AI 可能将其归入较早模块的问题。现在会按教材大纲顺序，将题目定位到解题中实际使用且位置更靠后的模块。

## 功能更新

- 组卷工作台的每道题现在都可以单独显示或隐藏参考答案与解析，便于教师在选题时快速核对。
- 答案默认保持收起，只在首次展开时加载，重复查看会使用当前会话缓存，不会反复请求数据。
- 支持一键“收起全部答案”；无答案的题目会直接标识为“暂无答案”。
- 答案中的数学公式和图片可正常渲染；答案的展开状态仅用于选题查看，不会改变右侧试卷内容，也不会影响 PDF、Word 或 LaTeX 导出。
