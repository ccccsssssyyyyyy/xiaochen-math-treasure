# 本地化数学题库系统：项目目录重构与模块解耦整理计划

> **文档状态**：待执行规划方案  
> **创建时间**：2026-08-08  
> **适用范围**：后端架构解耦、根目录精简、Git 版本演进与跨平台 Release 打包兼容性保障  

> **2026-08-08 实施说明**：经代码核验后，本方案已调整为低风险渐进式重构，不直接执行大规模目录搬迁。第一阶段已完成 `mathbank/paths.py` 统一绝对路径、四套教材共享 JSON 与预设接口、`mathbank/prompts.py` 提示构建器、Release 包含共享模块、开发依赖及架构回归测试；根目录 `main.py`、CLI 与启动命令继续兼容。后续路由/服务拆分应建立在本阶段全绿基线上分批实施。

---

## 一、 重构背景与现状分析

随着系统功能的不断演进与丰富，目前项目根目录已累积了 **30+ 个不同性质的文件**，存在以下核心痛点：

1. **`main.py` 单文件过度膨胀**（5,103 行，约 245 KB）：
   - **大块静态数据堆积**（约 650 行）：硬编码了人教 A、人教 B、苏教、沪教四套完整的高中数学树状大纲及版本检测与跨大纲翻译算法。
   - **大模型提示词长篇嵌入**（约 450 行）：OCR 图文识别、TikZ 几何绘图与纠错、AI 解题推导规范、智能拆卷与智能组卷等 System Prompts 散落在各业务函数中。
2. **根目录文件职责混杂**：
   - 包含了临时排版对比测试生成的截图文件（如 `hangindent_test.png`、`noindent_comparison_test.png` 等）。
   - 包含了历史失败重试遗留的临时数据库文件（如 `math_question_bank.db.failed_attempt` 等）。
   - 包含了数据迁移与维护脚本（如 `migrate_fillin.py`、`build_release.py` 等）。

---

## 二、 目标目录架构设计

按照**“关注点分离（Separation of Concerns）”**与**“主干稳固、分支下沉”**的工程规范，规划全新的目录结构如下：

```text
题库项目/
├── 启动题库系统.command / .bat        # [根目录保留] 跨平台一键快捷启动器
├── main.py                          # [根目录保留] FastAPI 服务核心入口与路由调度 (保持启动命令不变)
├── requirements.txt                 # [根目录保留] Python 依赖声明
├── README.md / AGENTS.md / CLAUDE.md# [根目录保留] 项目说明与 AI 协同指南
├── LICENSE                          # [根目录保留] 开源许可证
│
├── core/                            # 🌟 [新建] 核心业务支撑包 (下沉的分支 py 模块)
│   ├── __init__.py
│   ├── database.py                  # 数据库 ORM 模型定义与会话引擎
│   ├── curriculums.py               # 4套教材大纲字典、版本检测与跨大纲路由算法
│   ├── prompts.py                   # OCR、TikZ、AI 解题、拆卷、组卷等所有提示词集中管理
│   ├── paper_helper.py              # 试卷 LaTeX 编译、多图排版与留白计算引擎
│   └── sync_helper.py               # 数据库异步备份与 AI 专属题库同步工具
│
├── scripts/                         # 🌟 [新建] 运维、迁移与终端 CLI 工具集
│   ├── search_questions.py          # 本地终端 SQL 模糊检索与 AI 备课工具
│   ├── migrate_fillin.py            # 历史填空题下划线规范化批量迁移工具
│   ├── migrate_choice_parentheses.py# 选择题题干末尾括号净化工具
│   └── build_release.py             # Windows / macOS 便携绿色一键包构建打包脚本
│
├── docs/                            # 🌟 [新建] 项目文档与设计资产
│   └── images/                      # README 预览图与产品架构展示图 (screenshot1.png, screenshot2.png)
│
├── data_backup/                     # [已有] 数据持久化备份目录
│   ├── archive/                     # 🌟 [新建] 历史版本与废弃临时 .db 归档
│   ├── questions_backup.json        # 全量数据库 JSON 备份
│   └── questions_library.md         # AI 专属只读题库 (安全防泄露)
│
├── static/                          # [已有] 前端静态资源 (HTML, JS, CSS, 本地字体与 KaTeX 库)
└── tests/                           # [已有] 自动化测试套件
    └── artifacts/                   # 🌟 [新建] 排版测试与图像断言生成的临时验证图片
```

---

## 三、 兼容性深度评估（Git 用户 & Release 用户）

### 1. 对 Git Clone / Pull 用户的兼容性保障
* **Git 重命名追踪机制**：
  Git 采用内容哈希（Blob Hash）追踪机制，移动文件时 Git 会记录为 `rename from a.py to core/a.py (similarity 100%)`。
* **保留 100% Commit 历史**：
  后续在子目录下执行 `git log --follow core/paper_helper.py`，依然能完整回溯该文件自创建以来的所有历史提交。
* **用户执行 `git pull` 的表现**：
  - 本地无冲突修改的用户：`git pull` 瞬间自动创建新文件夹并完成文件迁移，**零报错、零冲突**。
  - 本地有修改的用户：Git 三方合并引擎（3-way merge）能基于 Rename 记录自动将用户修改合并到新路径下的文件中。

### 2. 对 Release 下载用户的兼容性保障
* **现有旧版 Release 用户**：
  Release 包是独立自包含的绿色便携环境，继续独立运行，**100% 毫无影响**。
* **未来升级新版 Release 的用户**：
  用户下载最新 Release 解压后，仅需将旧版目录中的 `math_question_bank.db`（数据库）与 `.env`（API 配置）复制进新文件夹，双击启动器即可无缝运行，**历史数据 100% 完整保留**。
* **打包脚本升级 (`build_release.py`)**：
  更新打包脚本，将 `core/` 目录整包复制入构建目录，确保以后每次编译产出的 **Windows x64 一键包** 与 **macOS 一键包** 均为最新整洁架构。

### 3. 启动命令零破坏原则
* 主服务入口文件依然命名为根目录的 `main.py`。
* `启动题库系统.command` 与 `启动题库系统.bat` 中的核心启动命令 `uvicorn main:app` 保持不变，**完全无需重构启动器**。

---

## 四、 具体拆解与重构任务清单

### 1. 核心模块下沉 (`core/`)
- [ ] **新建 `core/curriculums.py`**：
  - 移入 `RENJIAO_A_CURRICULUM`, `RENJIAO_B_CURRICULUM`, `SUJIAO_CURRICULUM`, `HUJIAO_CURRICULUM`。
  - 移入 `get_active_version_code()` 与 `route_chapter()` 路由算法。
- [ ] **新建 `core/prompts.py`**：
  - 移入 `COMMON_OCR_PROMPT`, `ILLUSTRATION_BOX_PROMPT`。
  - 移入 `TIKZ_DRAW_SYSTEM_PROMPT`, `TIKZ_CORRECT_SYSTEM_PROMPT`。
  - 移入 AI 解题推导、智能拆卷与智能组卷的 Prompt 构建器。
- [ ] **平移工具模块至 `core/`**：
  - 移动 `database.py` -> `core/database.py`。
  - 移动 `paper_helper.py` -> `core/paper_helper.py`。
  - 移动 `sync_helper.py` -> `core/sync_helper.py`。
- [ ] **简化 `main.py`**：
  - 通过 `from core.curriculums import ...`、`from core.prompts import ...` 等引入各模块。
  - `main.py` 瘦身 1,100+ 行，专注 FastAPI 路由与中间件。

### 2. 运维与辅助工具归类 (`scripts/`)
- [ ] 移动 `migrate_fillin.py` -> `scripts/migrate_fillin.py`。
- [ ] 移动 `migrate_choice_parentheses.py` -> `scripts/migrate_choice_parentheses.py`。
- [ ] 移动 `build_release.py` -> `scripts/build_release.py`（并同步更新打包复制路径）。
- [ ] 移动 `search_questions.py` -> `scripts/search_questions.py`（在根目录保留极简快捷入口或在文档中更新说明）。

### 3. 临时文件与历史备份清理
- [ ] 移动 `screenshot1.png`, `screenshot2.png` -> `docs/images/`。
- [ ] 归档临时测试数据库 `math_question_bank.db.failed_attempt` 等 -> `data_backup/archive/`。
- [ ] 清理或归档此前排版对比生成的临时测试图（`hangindent_test*.png`, `noindent_comparison_*.png`, `test_preview*.png`）。

---

## 五、 实施路线图与验证标准

### 质量验证标准
1. **自动化测试**：运行 `pytest`，确保所有 18 项现有测试 100% 通过（`18 passed`）。
2. **前后端接口验证**：
   - 验证题库 CRUD 与搜索列表拉取。
   - 验证大纲切换（人教A/B、苏教、沪教）及跨大纲增量迁移。
   - 验证 OCR 识图、TikZ 几何绘图、AI 解题推导。
   - 验证试卷排版 A4 实时预览与 PDF/ZIP 导出。
3. **打包脚本验证**：运行 `python3 scripts/build_release.py`，验证生成的 Windows/macOS 发行包能够正常解压并打包完整依赖。
4. **文档同步**：同步更新 `AGENTS.md` 和 `CLAUDE.md` 中的目录指引。
