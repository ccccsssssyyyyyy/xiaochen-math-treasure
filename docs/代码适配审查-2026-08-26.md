# 代码适配审查报告 — 2026-08-26

> 审查对象：「小陈的数学宝藏」个人备课工具（派生自 math-question-bank，FastAPI + 原生 JS 前端，端口 8000）
> 审查目标：核对当前代码是否适配已确认的需求
> 审查者：WorkBuddy（代码审查，未改动任何代码）

## 1. 审查范围与方法

- 读取项目权威进度 `MEMORY.md` 与当日/前日日志（`2026-08-26.md` / `2026-08-25.md`）。
- 静态校验：`.venv` Python `py_compile`（全部改动 .py + 新模块），受管 node `node --check`（全部改动 .js）。
- 回归测试：运行项目自带测试套件作为「需求落地」的客观证据。
- 运行态：检查 8000 端口服务存活与加载的代码版本；检查未提交改动中的残留引用风险。

## 2. 需求清单 vs 落地状态

| # | 需求（已与用户确认） | 落地 | 验证手段 |
|---|---|---|---|
| 1 | 拆卷步骤进度条 + 错误定位 | ✅ | 日志/代码确认 |
| 2 | 题目多标签（知识点+解题方法）+ 题库多选筛选 | ✅ | filter_test 15/15 |
| 3 | 标签搜索式多选 + 右侧题库/预览双视图 | ✅ | 代码确认 |
| 4 | 多文件批量上传修复（作用域 bug） | ✅ | 多套 jsdom 回归 |
| 5 | 多文件手动截图按来源取图 | ✅ | pdfPageImagesMap 断言 |
| 6 | 拆解结果审查筛选（未导入/已导入/全部） | ✅ | filter_test 15/15 |
| 7 | 已导入文档免重复拆解 + 仍要拆解兜底 | ✅ | dedup_skip_test |
| 8 | 多文件队列隐藏试卷标题框 | ✅ | title_visibility_test |
| 9 | 拆解队列卡死自愈 + 部分完成可导入 | ✅ | timeout/partial 测试 |
| 10 | 拆卷审查目录竖栏标签 | ✅ | catalog_label_test 11/11 |
| 11 | 编辑面板标签串题修复 | ✅ | edit_tag_input_test 12/12 |
| 12 | 单题 OCR 后 AI 自动全量分类 + 去弹窗 | ✅ | ocr_auto_classify_test 13/13 |
| 13 | 免费模型自动路由（难度评估→自动选） | ✅ | free_model_routing_test 10/10 |
| 14 | TikZ 前端入口隐藏（后端保留） | ✅ | 死代码，空值保护 |
| 15 | Word 拆卷乱码清洗 + 红条警告 + 网页内看原卷 | ✅ | 端到端实测（专题02 通过）|
| 16 | docx 原卷预览放大修复（DPI 250 + 真实像素基准）| ✅ | 静态+重启验证 |
| 17 | 「从队列移除」误弹文件选择器修复 | ✅ | node --check + 回归 |
| 18 | 组卷「全部试题」定高虚拟滚动 + 答案弹窗 | ✅ | 2500 题库 jsdom 冒烟 |
| 19 | 虚拟列表公式未渲染修复（补单 `$` 分隔） | ✅ | node --check + 冒烟 |
| 20 | 组卷 Word 导出公式红框 → 装 Pandoc + 优化兜底 | ✅ | 真实端点 x-word-native-formulas:74 |

**唯一未实现的需求（明确搁置）**：
- **讲义制作功能**：`paper.js` / `database.py` 仅有 `handout` 枚举/占位，需求澄清文档未落盘，按用户约定尚未开发。

## 3. 验证证据（全部绿灯）

```
py_compile  : main.py / free_model_routing.py / content_locks.py /
              docx_helper.py / prompts.py / word_export_helper.py  → OK
node --check: api.js / editor.js / import.js / ocr.js / paper.js       → OK
Python 测试 : tools/free_model_routing_test.py        → 10/10 passed
前端 jsdom  : filter_test.js (15) catalog_label_test.js (11)
              edit_tag_input_test.js (12) ocr_auto_classify_test.js (13)
                                                        → 合计 51/51 passed
服务运行态 : PID 11353 监听 127.0.0.1:8000，已加载截至 Word 导出修复的全部后端代码
```

## 4. ⚠️ 关键风险：整个工作树未提交（最高优先级）

- `git status` 显示 **12 个文件已修改 + 6 项未跟踪**（含新增模块 `mathbank/free_model_routing.py`、测试、3 份需求文档），合计 **+1092 / −976 行**。
- 最近一笔 commit 停在 **2026-08-25 21:50**（`c7745f4`）。**2026-08-25 晚间 + 2026-08-26 当天的全部功能（免费路由、TikZ 隐藏、docx 增强、原卷放大、移除弹窗修复、虚拟滚动、公式渲染修复、Word 导出修复）均在工作树、未入库。**
- 风险：无版本控制安全网。一旦工作树或 `.venv` / 数据库损坏，当日全部工作可能丢失；且无法追溯「哪次改动引入了问题」。
- 后端已多次重启加载新代码（PID 11353），但**前端-only 的改动（虚拟滚动、移除弹窗、公式渲染）需用户在浏览器 `Cmd+Shift+R` 强刷才生效**，若未强刷则浏览器仍是旧 JS。

## 5. 已知缺口 / 技术债（均为用户知情并接受的取舍）

1. **免费解题模型仅 UI 可配，路由未接**（按"拆解+分类"范围，解题保持付费）。
2. **TikZ 前端函数作为死代码保留**（`import.js` 的 `extractTikzCodeFromTextarea`/`*Tikz*` 系列），全部用 `if (getElementById(...))` 空值保护，**不报错、不影响功能**，恢复 UI 时只需加回 HTML 与调用点。
3. **上游 v2.1.5 明确不 merge**：上游删除了本项目 OCR 自动分类依赖的 `knowledge_list`/`solve_method` 两列，直接 merge 会破坏现有功能。
4. **讲义制作功能**尚未启动（见第 2 节）。

## 6. 结论

**代码在功能层面已适配全部已确认需求**——20 项需求全部落地，静态校验与回归测试全绿，运行服务与代码一致。

**唯一需要立即决策的是工程安全层面**：当前所有 2026-08-26 的改动都游离在版本控制之外。建议按 MEMORY.md 第 7 节约定，**在用户明确授权后，按功能拆分提交**（如：免费路由 / TikZ 隐藏 / docx 增强 / 原卷放大 / 队列移除修复 / 虚拟滚动 / Word 导出修复），并补一次 `git tag` 锚定「当前可用状态」，避免单笔巨型 commit 难以回滚。

---

*本审查未执行任何代码/配置改动，符合项目「先对齐再开发」约定。*
